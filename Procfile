# Procfile untuk Railway/Render
# Main web server
web: php artisan serve --host=0.0.0.0 --port=${PORT:-8080}

# Background queue worker (uncomment untuk production)
# worker: php artisan queue:work --tries=3 --timeout=90
